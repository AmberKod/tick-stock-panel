# 新闻热点分析工作区 功能页面设计方案

**日期**：2026-09-15
**场景**：设计研究（产品评审 + 视觉布局双线并行）
**参与成员**：产品官（gstack-product-reviewer） + 设计师（gstack-designer） + 主理人汇编

---

## 📌 TL;DR（执行摘要）

- 整体结论：🟢 方案成型，可进入实施——但要先做**第 0 批前置**（数据链路验证），否则一切是空中楼阁
- 两个「假功能」被识别：① 热点生产数据从未产出（`data/hotspot/` 不存在，调度窗口 09:05-15:35 与服务晚间启动时段错配，同 market_daily 病根）；② 五段生命周期是硬编码常数（`akshare_source.py:297-300` trend/persistence/cooling 恒 0，`hk_us_source.py:468-473` 首日恒 None）——而 history JSONL 每次同步都在攒观测点，**引擎没接、数据在攒**
- 定位拍板：**不做新闻流，做「盘中主线雷达 + 尾盘决策的板块层输入」**
- 布局推荐：**变体 B 主从工作台**（左扫描列表 + 右常驻详情，零弹窗摩擦）
- 交互式 mockup 已产出，可直接体验

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| 方案状态 | 🟢 设计完成，待 Amber 拍板 2 个决策点后开工 |
| MVP 工作量 | 第 0 批 0.5-1 天 + MVP-1 约 3 天（后端 1-2 天 + 前端 1 天） |
| 决策点 1 | 详情区是否保留「新闻模块」占位（产品官主张砍，设计师建议留空态） |
| 决策点 2 | 布局变体 A（保守增强）vs B（主从工作台，推荐） |
| 交付物 | 交互式 mockup + 布局设计文档 + 本方案 |

---

## 1. 各成员核心结论

### 🔍 产品官（功能框架）
- 核心判断：工作区的问题不是缺功能，是**两个假功能**——数据链路从未跑通 + 生命周期五段是摆设（history 引擎没接，补齐不需要新数据源，是纯后端加工）
- 关键建议：不做新闻（板块涨幅+成分股量价+涨停事实已是新闻的滞后投影，新闻是更 noisy 的同源信号）；死区处置 = timeline 激活为热度轨迹 / route 砍 / news_search 砍；下游联动按「个股详情 → Regime 主线行 → 梯队交叉标注 → 筛选器闭环」排序推进
- 四场景工作流：盘前（昨日定版榜+连续性）→ 盘中（Δ 加速降温）→ 尾盘 14:30-14:50（stage 过滤主线清单喂筛选器）→ 盘后（单主题完整轨迹）

### 🎨 设计师（布局视觉）
- 核心判断：全站设计体系完整（§6.0 暗色 token + Regime 范式），Hotspots 页停留在旧版式；最大交互痛点 = 全屏 Modal 抽屉与「横向扫描多主题」节奏冲突
- 关键建议：变体 B 主从工作台——Regime 同款页头（接全局 useMarket）+ 阶段分布条 + 左右常驻分栏（左=搜索/阶段 chips/排序/紧凑列表，右=指标卡/热度轨迹 SVG/关联链路/成分股表）；≤1100px 降级堆叠（等价变体 A 兜底）
- 成果：交互式 mockup（点行选中/搜索/阶段筛选/排序/键盘↑↓/市场切换/设计标注开关）+ 实现者 6 条一致性要点

---

## 2. 综合方案（功能 × 布局合并）

### 2.1 MVP 分期（产品官框架）

| 批次 | 内容 | 工作量 | 依赖 |
|------|------|--------|------|
| **第 0 批（前置）** | A 同步链路跑通验证 + I 删 `/news` 占位页* | 0.5-1 天 | 无 |
| **MVP-1（核心价值）** | B 生命周期激活（读 history 算 trend/persistence/cooling/observations）+ C 列表增强（Δ 列/连续天数/stage 筛选）+ D 市场切换接全局 marketContext | 后端 1-2 天 + 前端 1 天 | 第 0 批 |
| **MVP-2（联动）** | E 成分股跳个股分析 + F 详情热度轨迹 + Regime 主线 top3 行 | 前后端各 0.5 天 ×2 | MVP-1 |
| **MVP-3（可选）** | G 港美实时刷新说明 + H 盘后复盘周级回顾 + 筛选器闭环 | 1-2 天 | MVP-2 |

*删除 `/news` 占位页受决策点 1 影响，见下。

### 2.2 布局信息层级（变体 B）

```
页头（Regime 同款渐变竖条 + 全局市场切换 pill 组 + 同步按钮）
├─ 元信息条（精简：数据源 / 主题数 / 最近同步 / 质量徽标）
├─ 阶段分布条（五段生命周期堆叠条 + 新晋/退潮预警计数）
└─ 主从分栏
   ├─ 左·扫描区（约 45%）：搜索框 + 阶段 chips + 排序 + 紧凑列表
   │   （Δ 列 + stage 徽标 + 连续天数是视觉焦点；行可键盘↑↓）
   └─ 右·详情区（约 55%）：主题名/阶段/涨跌幅 → 5 指标卡（热度/趋势/
       持续/降温/连续天数）→ 20 日热度轨迹 SVG + 阶段色带 → 关联链路
       chips → 成分股表（可点击行 → 个股分析）→ [新闻模块? 见决策点 1]
```

### 2.3 两成员分歧仲裁（决策点 1：新闻模块）

| 立场 | 内容 |
|------|------|
| 产品官 | 砍：真新闻源 ROI 低（akshare 无稳定 API）、新闻是板块信号的 noisy 同源投影、违背 fail-closed；`/news` 占位页删除 |
| 设计师 | 留：详情区尾部放「新闻空状态占位」模块，`/news` 定位全局新闻流与本页不重复 |

**主理人建议**：倾向产品官方案（砍掉新闻模块 + 删 `/news`），理由是 fail-closed 原则与 N=1 用户的工作流（决策不需要读新闻原文）。但 mockup 里已画了空态占位，**保留与否由 Amber 拍板**——保留成本仅一个空态组件，砍掉的沉没成本也仅此而已。

---

## ✅ 行动清单

| # | 行动 | 负责方 | 紧急度 | 期望完成 |
|---|------|--------|--------|---------|
| 1 | Amber 拍板：决策点 1（新闻模块去留）+ 决策点 2（布局变体 A/B） | Amber | P0 | 本轮 |
| 2 | 第 0 批：盘中手动触发一次 hotspot sync 验证链路 + akshare 依赖检查 | Gu | P0 | 拍板后 |
| 3 | MVP-1 开发：生命周期引擎接 history + 列表增强 + marketContext 接入 | Gu | P0 | 第 0 批后 |
| 4 | 布局改造按 mockup 变体 B 落地（先 tsc 校验，用户侧 build 验收） | Gu | P1 | MVP-1 后 |

---

## ⚠️ 待完善 / 已知局限

- mockup 为纯静态假数据演示，未接真实 API；实施时以 `deliverables/gstack/hotspot-workspace-layout-design.md` 第 5 节 6 条一致性要点为准
- Kill Criteria（产品官）：第 0 批若东财板块接口连续多日 degraded 且无替代源 → 工作区降级为港美行业聚合工具；MVP-1 上线一周使用 < 3 次 → 停投 MVP-2，先访谈
- 本方案与港股补跑无耦合，可并行推进

---

## 📚 成员产出索引

- gstack-product-reviewer（产品官）：功能框架设计报告（TL;DR 之上的完整版含工作流映射/区块清单/死区决策/Kill Criteria，见本文件 §1 摘要 + 对话记录）
- gstack-designer（设计师）：`deliverables/gstack/hotspot-workspace-layout-design.md`（完整设计文档）+ `deliverables/gstack/mockups/hotspot-workspace-master-detail.html`（交互式 mockup，52KB）

---

> 本报告由软件工坊 AI 协作生成，关键决策请由工程负责人复核。
